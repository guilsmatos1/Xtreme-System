# Token Efficiency Retrospective — GUI-349 SQL pagination ordering

_Codex session: ses_0654593f9ffeN3WL0MaTwMcGF2 · worktree: /Users/guilsmatos/orca/workspaces/xtreme-system/GUI-349 · model: gpt-5.5 · Date: 2026-07-25_

## Execution profile (real tokens from the session table)
- input: 84667 · output: 5941 · reasoning: 2650 · cache_read: 996864 · cache_write: 0 · cost: $0.0
- Assistant steps: 39 · Tools: bash x42, read x7, grep x4, apply_patch x4, skill x2, glob x1
- Largest outputs / rereads / truncations: failed commit hooks 12977 chars; `test_route_factories_ui.py` reads of 9476 chars and 3212 chars; `crud_ui/query.py` read of 7023 chars; `crud_ui/sort_fields` grep of 6024 chars; 6 truncated reads.

## Top 5 improvements (sorted by estimated savings)

### 1. Local preflight before commits with expensive hooks
- **Pattern:** avoidable round trip / large hook output.
- **Evidence:** recipe (d/e): `rtk git add . && rtk git commit -m "stabilize paginated sql ordering"` produced 12977 chars of output when `ruff`, `vulture`, and `pytest` failed; then `rtk git commit -m ...` produced another 2728 chars for a single long line.
- **Observed cost:** ~3926 estimated direct-output tokens, with a multiplicative effect on `cache_read` because the errors entered context before several fix/merge steps.
- **Harness target (Codex):** AGENTS.md or skill `commit-merge`.
- **Solution:** before committing, when there are new tests or Python edits, run a short preflight: `rtk ruff check <files>`, `rtk ruff format --check <files>`, and the already used focused test; only then trigger `git commit` with full hooks.
- **Estimated savings:** ~18000 tokens including avoided cache_read.
- **Same-result test:** the same lint/test errors would have been detected before commit, with smaller output and without changing the final fix or skipping hooks.

### 2. Read smaller slices of large test files
- **Pattern:** large output early / reading more than needed.
- **Evidence:** recipe (e/c): `tests/test_route_factories_ui.py` was read twice; the largest read returned 9476 chars for lines 280-539, although the required change was inserting a test after the function around 280-310.
- **Observed cost:** ~2370 estimated tokens in the largest read, reprocessed in later steps through cache_read.
- **Harness target (Codex):** AGENTS.md.
- **Solution:** to insert a test near a grep-located target, read at most `line-40..line+80`; expand only if imports/helper classes are missing.
- **Estimated savings:** ~9000 tokens.
- **Same-result test:** the test insertion only needed imports and the neighboring block; most of lines 328-539 did not influence the implementation.

### 3. Avoid broad CRUD UI scans after the target file is identified
- **Pattern:** main-worker scan / noisy grep.
- **Evidence:** recipe (g/e): `grep "_query_sorted_list|crud_ui|sort_fields"` returned 75 matches and 6024 chars after `crud_ui/query.py` had already been identified as the central point.
- **Observed cost:** ~1506 estimated direct-output tokens with low incremental value.
- **Harness target (Codex):** AGENTS.md or graphify.
- **Solution:** when an initial grep has already found the exact helper, read the target file and search callers only if the signature must change; for a broad map, delegate to a subagent/graphify and return only the 3-5 relevant findings.
- **Estimated savings:** ~6000 tokens.
- **Same-result test:** the fix was made inside `_query_sorted_list`; the full list of 75 occurrences did not change the patch.

### 4. Use model metadata before exploratory scripts
- **Pattern:** failed exploratory commands.
- **Evidence:** recipe (d/e): three Python attempts to inspect SQL failed due to environment/imports before revealing that the test expected the wrong table; one produced a 4245-char traceback.
- **Observed cost:** ~1600 estimated direct-output tokens.
- **Harness target (Codex):** AGENTS.md.
- **Solution:** before running an exploratory SQLAlchemy script, grep model metadata such as `__tablename__` when the question is a table name; run a script only if the information is not in code.
- **Estimated savings:** ~5000 tokens.
- **Same-result test:** `grep "__tablename__|class Investidor"` found `__tablename__ = "investidor"`, enough information to fix the assertion without the failed scripts.

### 5. Consolidate post-merge inspection without detailed diff when status is enough
- **Pattern:** noisy commit/merge command.
- **Evidence:** recipe (d/e): after a concurrent merge, `rtk git diff HEAD -- bases/... tests/...` produced 3556 chars to confirm GUI-349 changes were staged; `git merge-base` and `git diff --cached --name-only/stat` already showed the situation.
- **Observed cost:** ~889 estimated direct-output tokens, reintroduced into context at the end of the session.
- **Harness target (Codex):** skill `commit-merge`.
- **Solution:** during merge conflict/convergence, first use `git merge-base --is-ancestor`, `git diff --cached --name-only`, and `git diff --cached --stat`; open a detailed diff only for unexpected files or a real conflict.
- **Estimated savings:** ~2500 tokens.
- **Same-result test:** the decision was to commit the four expected staged files; `--name-only` and `--stat` already proved that without requiring the full diff.

## Total estimated savings
~40500 tokens (~4% of session cache_read), with no change to the delivered result.

---

## Pipeline blocks (0004 format)

Improvement #1: Local preflight before commits with expensive hooks
- Problem: Failed commit hooks dumped ~15705 chars of errors that could have been detected earlier with smaller commands.
- Solution: AGENTS.md/skill `commit-merge`: run `ruff check`, `ruff format --check`, and the focused test before Python commits with new tests.
- Estimated savings: ~18000 tokens

Improvement #2: Read smaller slices of large test files
- Problem: Reading 260 lines of `test_route_factories_ui.py` brought ~9476 chars when the neighborhood around the target test was enough.
- Solution: AGENTS.md: after grep with a line number, read `line-40..line+80` and expand only on demand.
- Estimated savings: ~9000 tokens

Improvement #3: Avoid broad CRUD UI scans after the target file is identified
- Problem: Broad CRUD UI grep returned 75 matches/~6024 chars without changing the solution.
- Solution: AGENTS.md/graphify: after finding the exact helper, read the target file; delegate broad maps to a subagent/graphify.
- Estimated savings: ~6000 tokens

Improvement #4: Use model metadata before exploratory scripts
- Problem: Failed Python scripts produced tracebacks to discover information already available in `__tablename__`.
- Solution: AGENTS.md: check simple model metadata with grep before running exploratory SQLAlchemy scripts.
- Estimated savings: ~5000 tokens

Improvement #5: Consolidate post-merge inspection without detailed diff when status is enough
- Problem: Detailed post-merge diff produced ~3556 chars even though `--name-only`/`--stat` were enough.
- Solution: skill `commit-merge`: use `merge-base`, `diff --cached --name-only`, and `--stat` before opening a full diff.
- Estimated savings: ~2500 tokens
