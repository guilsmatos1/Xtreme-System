# Retrospectiva de Eficiência de Tokens — GUI-349 SQL pagination ordering

_Sessão Opencode: ses_0654593f9ffeN3WL0MaTwMcGF2 · worktree: /Users/guilsmatos/orca/workspaces/xtreme-system/GUI-349 · modelo: gpt-5.5 · Data: 2026-07-25_

## Perfil de execução (tokens reais da tabela session)
- input: 84667 · output: 5941 · reasoning: 2650 · cache_read: 996864 · cache_write: 0 · custo: $0.0
- Passos (assistant): 39 · Ferramentas: bash x42, read x7, grep x4, apply_patch x4, skill x2, glob x1
- Maiores outputs / re-leituras / truncamentos: commit com hooks falhos 12977 chars; leitura de test_route_factories_ui.py 9476 chars e 3212 chars; leitura de crud_ui/query.py 7023 chars; grep de crud_ui/sort_fields 6024 chars; 6 reads truncados.

## As 5 melhorias (ordenadas por economia estimada)

### 1. Preflight local antes de commit com hooks caros
- **Padrão:** ida-e-volta evitável / output grande de hook.
- **Evidência:** recipe (d/e): `rtk git add . && rtk git commit -m "stabilize paginated sql ordering"` gerou 12977 chars de output ao falhar em `ruff`, `vulture` e `pytest`; depois `rtk git commit -m ...` gerou mais 2728 chars por uma única linha longa.
- **Custo observado:** ~3926 tokens estimados de output direto, com efeito multiplicativo no `cache_read` porque os erros entraram antes de várias etapas de correção/merge.
- **Alvo de harness (Opencode):** AGENTS.md ou skill `commit-merge`.
- **Solução:** antes de commitar, quando houver testes novos ou edição Python, rodar um preflight curto: `rtk ruff check <arquivos>`, `rtk ruff format --check <arquivos>` e o teste direcionado já usado; só então acionar `git commit` com hooks completos.
- **Economia estimada:** ~18000 tokens incluindo cache_read evitado.
- **Teste de mesmo-resultado:** os mesmos erros de lint/teste teriam sido detectados antes do commit, com saída menor e sem mudar a correção final nem pular hooks.

### 2. Ler fatias menores de arquivos de teste grandes
- **Padrão:** output grande cedo / leitura acima do necessário.
- **Evidência:** recipe (e/c): `tests/test_route_factories_ui.py` foi lido duas vezes; a maior leitura retornou 9476 chars para linhas 280-539, embora a mudança necessária fosse inserir um teste após a função em torno de 280-310.
- **Custo observado:** ~2370 tokens estimados na maior leitura, reprocessados em passos seguintes via cache_read.
- **Alvo de harness (Opencode):** AGENTS.md.
- **Solução:** para inserir teste próximo a um alvo localizado por grep, ler no máximo `line-40..line+80`; só ampliar se imports/classes auxiliares estiverem ausentes.
- **Economia estimada:** ~9000 tokens.
- **Teste de mesmo-resultado:** a inserção do teste precisava apenas dos imports e do bloco vizinho; a maior parte das linhas 328-539 não influenciou a implementação.

### 3. Evitar varredura ampla de CRUD UI depois de arquivo-alvo identificado
- **Padrão:** varredura no worker principal / grep tagarela.
- **Evidência:** recipe (g/e): `grep "_query_sorted_list|crud_ui|sort_fields"` retornou 75 matches e 6024 chars após `crud_ui/query.py` já ter sido identificado como ponto central.
- **Custo observado:** ~1506 tokens estimados de output direto, com baixo valor incremental.
- **Alvo de harness (Opencode):** AGENTS.md ou graphify.
- **Solução:** quando um grep inicial já localizar o helper exato, ler o arquivo-alvo e buscar chamadas apenas se a assinatura precisar mudar; para mapa amplo, delegar a subagente/graphify e retornar só os 3-5 achados relevantes.
- **Economia estimada:** ~6000 tokens.
- **Teste de mesmo-resultado:** a correção foi feita dentro de `_query_sorted_list`; a lista completa de 75 ocorrências não alterou o patch.

### 4. Usar metadados do modelo antes de executar scripts exploratórios
- **Padrão:** comandos exploratórios falhos.
- **Evidência:** recipe (d/e): três tentativas de Python para inspecionar SQL falharam por ambiente/imports antes de revelar que o teste esperava a tabela errada; uma delas produziu 4245 chars de traceback.
- **Custo observado:** ~1600 tokens estimados de output direto.
- **Alvo de harness (Opencode):** AGENTS.md.
- **Solução:** antes de rodar script exploratório com SQLAlchemy, procurar `__tablename__` via grep no model quando a dúvida for nome de tabela; só executar script se a informação não estiver no código.
- **Economia estimada:** ~5000 tokens.
- **Teste de mesmo-resultado:** `grep "__tablename__|class Investidor"` encontrou `__tablename__ = "investidor"`, informação suficiente para corrigir a asserção sem os scripts falhos.

### 5. Consolidar inspeção pós-merge sem diff detalhado quando o status basta
- **Padrão:** comando tagarela no commit/merge.
- **Evidência:** recipe (d/e): após merge concorrente, `rtk git diff HEAD -- bases/... tests/...` gerou 3556 chars para confirmar que as mudanças de GUI-349 estavam staged; `git merge-base` e `git diff --cached --name-only/stat` já indicavam a situação.
- **Custo observado:** ~889 tokens estimados de output direto, reintroduzidos no contexto no final da sessão.
- **Alvo de harness (Opencode):** skill `commit-merge`.
- **Solução:** em conflito/convergência de merge, primeiro usar `git merge-base --is-ancestor`, `git diff --cached --name-only` e `git diff --cached --stat`; só abrir diff detalhado se houver arquivo inesperado ou conflito real.
- **Economia estimada:** ~2500 tokens.
- **Teste de mesmo-resultado:** a decisão tomada foi commitar os quatro arquivos staged esperados; `--name-only` e `--stat` já provavam isso sem exigir o diff completo.

## Total estimado economizado
~40500 tokens (~4% do cache_read da sessão), sem mudança no resultado entregue.

---

## Blocos para o pipeline (formato 0003)

Melhoria #1: Preflight local antes de commit com hooks caros
- Problema: Hooks de commit falhos despejaram ~15705 chars de erros que poderiam ser detectados antes em comandos menores.
- Solução: AGENTS.md/skill `commit-merge`: rodar `ruff check`, `ruff format --check` e teste direcionado antes de commits Python com testes novos.
- Economia estimada: ~18000 tokens

Melhoria #2: Ler fatias menores de arquivos de teste grandes
- Problema: Leitura de 260 linhas de `test_route_factories_ui.py` trouxe ~9476 chars quando bastava o entorno do teste alvo.
- Solução: AGENTS.md: após grep com linha, ler `line-40..line+80` e ampliar só sob demanda.
- Economia estimada: ~9000 tokens

Melhoria #3: Evitar varredura ampla de CRUD UI depois de arquivo-alvo identificado
- Problema: Grep amplo de CRUD UI retornou 75 matches/~6024 chars sem alterar a solução.
- Solução: AGENTS.md/graphify: depois de achar helper exato, ler arquivo-alvo; delegar mapas amplos a subagente/graphify.
- Economia estimada: ~6000 tokens

Melhoria #4: Usar metadados do modelo antes de executar scripts exploratórios
- Problema: Scripts Python falhos geraram tracebacks para descobrir informação disponível em `__tablename__`.
- Solução: AGENTS.md: checar metadados simples do model com grep antes de rodar scripts exploratórios SQLAlchemy.
- Economia estimada: ~5000 tokens

Melhoria #5: Consolidar inspeção pós-merge sem diff detalhado quando o status basta
- Problema: Diff detalhado pós-merge gerou ~3556 chars apesar de `--name-only`/`--stat` bastarem.
- Solução: skill `commit-merge`: usar `merge-base`, `diff --cached --name-only` e `--stat` antes de abrir diff completo.
- Economia estimada: ~2500 tokens
