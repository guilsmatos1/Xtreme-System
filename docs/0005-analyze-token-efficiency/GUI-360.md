# Token Efficiency — GUI-360
_Codex: 019f9f44-5a3e-7df1-b9db-789b19b34a1c · input: 1220143 · cached: 1150976 · output: 8503 · total: 1228646_

Improvement #1: Avoid repeated identical calls
- Problem: Call repeated 2 times for exec_command `graphify update .`, re-emitting already seen outputs.
- Evidence: duplicate_calls: exec_command `graphify update .`; 2 repetitions; ~386 estimated output tokens.
- Solution: AGENTS.md/tool strategy: before repeating an identical command, reuse the previous conclusion or explicitly change scope/arguments.
- Estimated savings: ~1500 tokens
- Same-result test: The final deliverable would use the same evidence already obtained; only the literal repetition would be removed.

Improvement #2: Preflight failure-prone commands
- Problem: Exploratory commands failed and generated context without directly contributing to the solution.
- Evidence: failed_calls: 6 failures; largest example exec_command `uv run ruff check components/xtreme_system/crud/search.py components/xtreme_system/cliente/core.py components/xtreme_system/compra/core.py components/xtreme_system/veiculo/core.py components/xtreme_system/venda/core.py components/xtreme_sys...` with ~160 estimated tokens.
- Solution: AGENTS.md/RTK: validate path, environment, and minimal command before running expensive variants; prefer static inspection when the question is about names/signatures.
- Estimated savings: ~1000 tokens
- Same-result test: The same fixes and tests would be preserved; the change only removes attempts that ended in error.

Improvement #3: Limit large-output reads to the necessary section
- Problem: One call brought a large output into context before there was proven need for the full content.
- Evidence: largest_outputs: exec_command `rg -n "busca|buscar|search|query|texto|textual|ilike|like\(" --glob '*.py' --glob '*.html' --glob '*.js' --glob '*.ts' .` returned 19172 chars (~4793 tokens).
- Solution: AGENTS.md/RTK: when a target is already located, read a small line window or use filters (`rg`, `sed -n`) before opening full outputs.
- Estimated savings: ~24000 tokens
- Same-result test: The final deliverable depends on the relevant section; expanding the read would remain allowed only if the small section were insufficient.

Improvement #4: Chain discovery with more specific queries
- Problem: The session made many tool calls, a sign of broad discovery before converging on the target.
- Evidence: tool_counts total ~51; main: exec_command x39, apply_patch x12.
- Solution: Graphify/AGENTS.md: for code tasks, query graphify or run a specific initial grep, then open only directly implicated files.
- Estimated savings: ~6000 tokens
- Same-result test: The same code area would be found, but with fewer intermediate scans and without reducing final validation.

Improvement #5: Reduce context persisted between worker stages
- Problem: The session accumulated a lot of cached context, increasing the cost of each later stage.
- Evidence: tokens: cached_input_tokens=1150976, input_tokens=1220143, output_tokens=8503.
- Solution: Worker prompt/script: summarize findings before test/commit phases and avoid reintroducing old diffs, logs, and reads into the main context.
- Estimated savings: ~11500 tokens
- Same-result test: The summary would preserve decisions, changed files, and verification commands, preserving the final deliverable.
